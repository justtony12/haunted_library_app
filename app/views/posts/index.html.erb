<h1>All Posts:</h1>

<ul>
  <% @posts.each do |post| %>
    <div class="post">
      <li><h5><%= link_to post.title, post_path(post) %> | <%= post.author.name %> | <%= post.genre.name %> |</h5></li>
    </div>
  <% end %>
</ul>